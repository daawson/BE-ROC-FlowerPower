<?php 
    include 'database.php';

    // When request comes from POST Submit
    if(isset($_POST['Submit'])) {       
        // Get all data
        // Check if email exists
        // Check if everything is Database-Safe and RegEx'ed.
        // Insert into database and redirect to homepage.
    }
?>